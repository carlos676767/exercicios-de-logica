const inverterStr = (nome) => {
  let pegar = "";
  for (let i = nome.length; i >= 0; --i) {
    pegar += nome[i];
  }
  return pegar.slice(9);
};

console.log(inverterStr("carlos"));
console.log(inverterStr("luis lobo"));
console.log(inverterStr("amor da minha vida"));

const percorrer = arr.map((data) => {
  const comprimento = data.length / 1;
  const n = numero - comprimento;
  console.log(n);
});

const arr = ['José', 'Lucas', 'Nádia', 'Fernanda', 'Cairo', 'Joana'];





