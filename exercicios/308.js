const array = [`carlos`, `ana`, `silva`];
let indice = array.indexOf(`carlos`);
console.log(indice);

if (indice !== -1) {
  console.log(`elemento encontrado`);
} else {
  console.log(`elemento nao encontrado`);
}
