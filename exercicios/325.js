const nome = `radar`
const inverter = nome.split(``).reverse().join(``)
console.log(inverter);

if (inverter === nome) {
    console.log(`e um palindromo`);
}else{
    console.log(`nao e `);
}

 "nome: carlos "
